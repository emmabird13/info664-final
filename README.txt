# plre-network
Utilizing python to scrape the PLRE.Folger database and convert the data into a json format for further analysis.

# Request Books
Before running request_books.py, check the PLRE.Folger website through the URL contained in the code ro verify the range of book IDs. 
If desired to separate the headers from the contents of the cell, change "span" to "td" in line 46 of request_books.py.

# Convert JSON to CSV
This script requires pandas to be installed.
